# Core Dependencies
typer>=0.12.0
playwright>=1.40.0
praw>=7.7.0
PyYAML>=6.0
SQLAlchemy>=2.0.0
cryptography>=42.0.0

# Playwright requires browser binaries to be installed separately via:
# playwright install chromium
